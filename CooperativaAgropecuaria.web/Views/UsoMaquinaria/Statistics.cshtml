@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<CooperativaAgropecuaria.web.Models.EstadisticasMaquinariaViewModel>

@{
    ViewData["Title"] = "Estadísticas de Uso de Maquinaria";
}

<div class="container mt-4">
    <h2 class="mb-4 text-center">Estadísticas de Uso de Maquinaria</h2>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Nombre de la Maquinaria</th>
                <th>Horas de Uso Total</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Any())
            {
                var index = 1;
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@(index++)</td>
                        <td>@item.NombreMaquinaria</td>
                        <td>@item.TotalHorasUso</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="3" class="text-center text-danger">No hay datos disponibles.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
